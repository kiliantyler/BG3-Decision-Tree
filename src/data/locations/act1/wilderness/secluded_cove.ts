import type { Sublocation } from '@/types/location'
import { Wilderness } from './wilderness'

export const SecludedCove: Sublocation = {
  id: 'secludedcove',
  name: 'Secluded Cove',
  location: Wilderness,
}
