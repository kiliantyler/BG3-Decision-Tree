import { Act } from '@/data/acts'
import type { Location } from '@/types/location'
import { Nautiloid } from '.'

export const NautiloidLoc: Location = {
  id: 'nautoloid_loc',
  name: 'Nautoloid',
  act: Act.I,
  region: Nautiloid,
}
