import { Act } from '@/data/acts'
import type { Location } from '@/types/location'
import { ShadowCursedLands } from '.'

export const MoonriseTowers: Location = {
  id: 'moonrise_towers',
  name: 'Moonrise Towers',
  act: Act.II,
  region: ShadowCursedLands,
}
