import { Act } from '@/data/acts'
import type { Location } from '@/types/location'
import { Shadowfell } from '.'

export const ShadowfellLoc: Location = {
  id: 'shadowfell',
  name: 'Shadowfell',
  act: Act.II,
  region: Shadowfell,
}
