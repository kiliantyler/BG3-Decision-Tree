import { Act } from '@/data/acts'
import type { Location } from '@/types/location'
import { UpperCity } from '.'

export const UpperCityLoc: Location = {
  id: 'uppercity',
  name: 'Upper City',
  act: Act.III,
  region: UpperCity,
}
