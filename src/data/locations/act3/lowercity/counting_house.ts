import { Act } from '@/data/acts'
import type { Location } from '@/types/location'
import { LowerCity } from '.'

export const CountingHouse: Location = {
  id: 'counting_house',
  name: 'Counting House',
  act: Act.III,
  region: LowerCity,
}
