import { Act } from '@/data/acts'
import type { Location } from '@/types/location'
import { LowerCity } from '.'

export const FlymmCargo: Location = {
  id: 'flymm_cargo',
  name: 'Flymm Cargo',
  act: Act.III,
  region: LowerCity,
}
