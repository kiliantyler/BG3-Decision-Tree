import { Act } from '@/data/acts'
import type { Location } from '@/types'
import { LowerCity } from '.'

export const DevilsFee: Location = {
  id: 'devils_fee',
  name: "Devil's Fee",
  act: Act.III,
  region: LowerCity,
}
