import { Act } from '@/data/acts'
import type { Location } from '@/types/location'
import { LowerCity } from '.'

export const BloomridgePark: Location = {
  id: 'bloomridge_park',
  name: 'Bloomridge Park',
  act: Act.III,
  region: LowerCity,
}
