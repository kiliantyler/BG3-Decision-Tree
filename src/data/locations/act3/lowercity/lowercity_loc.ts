import { Act } from '@/data/acts'
import type { Location } from '@/types/location'
import { LowerCity } from '.'

export const LowerCityLoc: Location = {
  id: 'lowercity_loc',
  name: 'Lower City',
  act: Act.III,
  region: LowerCity,
}
