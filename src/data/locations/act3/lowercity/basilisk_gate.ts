import { Act } from '@/data/acts'
import type { Location } from '@/types/location'
import { LowerCity } from '.'

export const BasiliskGate: Location = {
  id: 'basilisk_gate',
  name: 'Basilisk Gate Barracks',
  act: Act.III,
  region: LowerCity,
}
