import { Act } from '@/data/acts'
import type { Location } from '@/types/location'
import { Rivington } from '.'

export const RequisitionedBarn: Location = {
  id: 'requisitioned_barn',
  name: 'Requisitioned Barn',
  act: Act.III,
  region: Rivington,
}
