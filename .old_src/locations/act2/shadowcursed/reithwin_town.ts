import { Act } from '@/data/acts'
import type { Location } from '@/types'
import { ShadowCursedLands } from '.'

export const ReithwinTown: Location = {
  id: 'reithwin_town',
  name: 'Reithwin Town',
  act: Act.II,
  region: ShadowCursedLands,
}
