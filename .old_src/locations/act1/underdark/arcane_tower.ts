import { Act } from '@/data/acts'
import type { Location } from '@/types'
import { Underdark } from '.'

export const ArcaneTower: Location = {
  id: 'arcane_tower',
  name: 'Arcane Tower',
  act: Act.I,
  region: Underdark,
}
