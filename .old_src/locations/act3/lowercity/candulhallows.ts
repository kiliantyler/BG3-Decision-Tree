import { Act } from '@/data/acts'
import type { Location } from '@/types'
import { LowerCity } from '.'

export const Candulhallows: Location = {
  id: 'candulhallows',
  name: "Candulhallow's Tombstones",
  act: Act.III,
  region: LowerCity,
}
