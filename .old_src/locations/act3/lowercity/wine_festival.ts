import { Act } from '@/data/acts'
import type { Location } from '@/types'
import { LowerCity } from '.'

export const WineFestival: Location = {
  id: 'wine_festival',
  name: 'Wine Festival',
  act: Act.III,
  region: LowerCity,
}
