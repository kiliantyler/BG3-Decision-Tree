import { Act } from '@/data/acts'
import type { Location } from '@/types'
import { Underground } from '.'

export const UndergroundLoc: Location = {
  id: 'underground',
  name: 'Underground',
  act: Act.III,
  region: Underground,
}
